name: Release

on:
  push:
    branches: [master]

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            binary: peer-asssessment-report-generator
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            binary: peer-asssessment-report-generator.exe
          - os: macos-latest
            target: aarch64-apple-darwin
            binary: peer-asssessment-report-generator

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v6

      - name: Install Linux dependencies
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwayland-dev libxkbcommon-dev wayland-protocols \
            libx11-dev libxrandr-dev libxinerama-dev \
            libxcursor-dev libxi-dev libgl1-mesa-dev

      - name: Set Wayland cmake flag
        if: matrix.os == 'ubuntu-latest'
        run: echo "RAYLIB_CMAKE_FLAGS=-DUSE_WAYLAND=ON" >> $GITHUB_ENV

      - run: rustup target add ${{ matrix.target }}

      - run: cargo build --release --target ${{ matrix.target }}

      - uses: actions/upload-artifact@v7
        with:
          name: peer-asssessment-report-generator-${{ matrix.target }}
          path: target/${{ matrix.target }}/release/${{ matrix.binary }}

  release:
    needs: build
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v6

      - name: Read base version from Cargo.toml
        id: version
        run: |
          BASE=$(grep '^version' Cargo.toml | head -1 | sed 's/version = "\(.*\)"/\1/')
          echo "tag=${BASE}" >> $GITHUB_OUTPUT

      - uses: actions/download-artifact@v8
        with:
          path: artifacts

      - name: Rename binaries
        run: |
          mv artifacts/peer-asssessment-report-generator-x86_64-unknown-linux-gnu/peer-asssessment-report-generator   artifacts/peer-asssessment-report-generator-linux-x86_64
          mv artifacts/peer-asssessment-report-generator-x86_64-pc-windows-msvc/peer-asssessment-report-generator.exe artifacts/peer-asssessment-report-generator-windows-x86_64.exe
          mv artifacts/peer-asssessment-report-generator-aarch64-apple-darwin/peer-asssessment-report-generator        artifacts/peer-asssessment-report-generator-macos-arm64

      - name: Set executable bit
        run: |
          chmod +x artifacts/peer-asssessment-report-generator-linux-x86_64
          chmod +x artifacts/peer-asssessment-report-generator-macos-arm64

      - uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.version.outputs.tag }}
          name: v${{ steps.version.outputs.tag }}
          body: "Commit: ${{ github.sha }}"
          files: |
            artifacts/peer-asssessment-report-generator-linux-x86_64
            artifacts/peer-asssessment-report-generator-windows-x86_64.exe
            artifacts/peer-asssessment-report-generator-macos-arm64