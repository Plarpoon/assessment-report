name: Release

on:
  push:
    branches: [master]

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            binary: peer-assessment-report-generator
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            binary: peer-assessment-report-generator.exe
          - os: macos-latest
            target: aarch64-apple-darwin
            binary: peer-assessment-report-generator

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v6

      - name: Install Linux dependencies
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwayland-dev libxkbcommon-dev wayland-protocols \
            libx11-dev libxrandr-dev libxinerama-dev \
            libxcursor-dev libxi-dev libgl1-mesa-dev

      - name: Set Wayland cmake flag
        if: matrix.os == 'ubuntu-latest'
        run: echo "RAYLIB_CMAKE_FLAGS=-DUSE_WAYLAND=ON" >> $GITHUB_ENV

      - run: rustup target add ${{ matrix.target }}

      - run: cargo build --release --target ${{ matrix.target }}

      - uses: actions/upload-artifact@v7
        with:
          name: peer-assessment-report-generator-${{ matrix.target }}
          path: target/${{ matrix.target }}/release/${{ matrix.binary }}

  release:
    needs: build
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v6

      - name: Read base version from Cargo.toml
        id: version
        run: |
          BASE=$(grep '^version' Cargo.toml | head -1 | sed 's/version = "\(.*\)"/\1/')
          echo "tag=${BASE}" >> $GITHUB_OUTPUT

      - uses: actions/download-artifact@v8
        with:
          path: artifacts

      - name: Set executable bit and package Unix binaries
        run: |
          chmod +x artifacts/peer-assessment-report-generator-x86_64-unknown-linux-gnu/peer-assessment-report-generator
          chmod +x artifacts/peer-assessment-report-generator-aarch64-apple-darwin/peer-assessment-report-generator

          tar -czf artifacts/peer-assessment-report-generator-linux-x86_64.tar.gz \
            --transform 's|.*|Peer Assessment Report Generator/peer-assessment-report-generator|' \
            artifacts/peer-assessment-report-generator-x86_64-unknown-linux-gnu/peer-assessment-report-generator

          tar -czf artifacts/peer-assessment-report-generator-macos-arm64.tar.gz \
            --transform 's|.*|Peer Assessment Report Generator/peer-assessment-report-generator|' \
            artifacts/peer-assessment-report-generator-aarch64-apple-darwin/peer-assessment-report-generator

          mv artifacts/peer-assessment-report-generator-x86_64-pc-windows-msvc/peer-assessment-report-generator.exe \
             artifacts/peer-assessment-report-generator-windows-x86_64.exe

      - uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.version.outputs.tag }}
          name: v${{ steps.version.outputs.tag }}
          body: "Commit: ${{ github.sha }}"
          files: |
            artifacts/peer-assessment-report-generator-linux-x86_64.tar.gz
            artifacts/peer-assessment-report-generator-windows-x86_64.exe
            artifacts/peer-assessment-report-generator-macos-arm64.tar.gz